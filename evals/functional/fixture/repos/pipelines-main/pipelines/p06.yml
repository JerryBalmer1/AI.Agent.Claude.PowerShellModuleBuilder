# case-06  A template in the variables block, not in steps.
trigger: none
pool:
  vmImage: ubuntu-latest
variables:
  - template: templates/vars-common.yml
  - name: buildConfiguration
    value: Release
steps:
  - script: echo building $(buildConfiguration) on $(vmImageName)
