# case-08  Entry point into a cycle. cycle-a includes cycle-b, which includes
# cycle-a. This definition would fail if run. It is never run.
trigger: none
pool:
  vmImage: ubuntu-latest
steps:
  - template: templates/cycle-a.yml
