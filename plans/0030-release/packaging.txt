==============================================================================
PACKAGING EVIDENCE - pass 0030
Generated 2026-09-02T01:57:20Z
==============================================================================

1. PublishLocal, end to end. The staged marketplace is generated by the script
   and so proves only self-agreement; the COMMITTED marketplace is the file a
   stranger reads, and is now validated separately in the same run.

Build PublishLocal C:\__Code\__AI.Agent.Claude.PowerShellModuleBuilder\AI.Agent.Claude.PowerShellModuleBuilder\.build.ps1
Task /PublishLocal
Repository: C:\__Code\__AI.Agent.Claude.PowerShellModuleBuilder\AI.Agent.Claude.PowerShellModuleBuilder
Staging to: C:\__Code\__AI.Agent.Claude.PowerShellModuleBuilder\AI.Agent.Claude.PowerShellModuleBuilder\scratch\local-marketplace
Staged 14 skills and 2 commands.
Staged validation passed: both JSON files parse, the source path resolves, skills and commands are present.

Committed marketplace validated: C:\__Code\__AI.Agent.Claude.PowerShellModuleBuilder\AI.Agent.Claude.PowerShellModuleBuilder\.claude-plugin\marketplace.json
  marketplace name : psmodule-builder
  plugin entry     : psmodule 1.0.0  source './'
  version agrees with .claude-plugin/plugin.json.
claude CLI: not on PATH. Skipped the CLI schema check - the JSON checks above are what ran, and they are weaker.

Paste these two inside Claude Code (not in this shell):

  /plugin marketplace add C:\__Code\__AI.Agent.Claude.PowerShellModuleBuilder\AI.Agent.Claude.PowerShellModuleBuilder\scratch\local-marketplace
  /plugin install psmodule@psmodule-local

To remove it again:

  /plugin uninstall psmodule@psmodule-local
  /plugin marketplace remove psmodule-local

Nothing was pushed. Nothing outside scratch/ was written.
Done /PublishLocal 00:00:01.2940265
Build succeeded. 1 tasks, 0 errors, 0 warnings 00:00:01.4207008
PublishLocal exit: 0

==============================================================================
2. claude plugin validate - ATTEMPTED, absent, recorded not fatal.

   claude CLI is NOT on PATH in this environment, so the CLI schema check
   did not run. Recorded, not skipped silently, and not treated as fatal:
   the JSON checks that did run are named above and are weaker than the
   CLI would be. What is NOT claimed by this pass: that the marketplace
   passes the official schema validator. Nobody has run it.

==============================================================================
3. PublishReal - the guard now takes its LIVE path for the first time.

Build PublishReal C:\__Code\__AI.Agent.Claude.PowerShellModuleBuilder\AI.Agent.Claude.PowerShellModuleBuilder\.build.ps1
Task /PublishReal
GUARD: passed. A committed marketplace.json is present.

  marketplace : psmodule-builder
  plugin      : psmodule 1.0.0
  repository  : https://github.com/JerryBalmer1/AI.Agent.Claude.PowerShellModuleBuilder

This script still publishes nothing. Publishing is the operator's verb,
run from the operator's own shell, on purpose. The checklist:

  1. Confirm the tree is clean and the pass is green.
       git status --porcelain          # expect no output

  2. Confirm the version in plugin.json is the one you mean to ship.
       current: 1.0.0

  3. Commit and push to the default branch, by fast-forward only.
       git push origin main

  4. The release tag is NOT yours to make - decision 0013 gives it to
     the release pass, which creates it only once its acceptance test
     is green, and never moves it afterwards. Confirm it is there:
       git ls-remote --tags origin 'v1.0.0*'
     If it is missing, the release pass did not finish. Do not tag by
     hand to cover for that - re-run the pass.

  5. Anyone installing then pastes these inside Claude Code. The add is
     PINNED TO THE TAG, so main can move without shipping to anyone:
       /plugin marketplace add JerryBalmer1/AI.Agent.Claude.PowerShellModuleBuilder@v1.0.0
       /plugin install psmodule@psmodule-builder

  6. Prove it cold: install on a machine that has never cloned this
     repository, run /psmodule:build on a scratch module, and record
     the transcript. An install nobody has done from outside is a claim.

No push was made by this script.
Done /PublishReal 00:00:00.9461031
Build succeeded. 1 tasks, 0 errors, 0 warnings 00:00:01.0592143
PublishReal exit: 0

==============================================================================
4. PublishReal still pushes nothing - checked, not asserted.

   Commands the parser finds INVOKED in Publish-Real.ps1:
     ConvertFrom-Json
     Get-Content
     Join-Path
     Resolve-Path
     Test-Path
     Write-Host

   No git, no Publish-Module, no nuget, no dotnet is INVOKED anywhere in
   the file. The one `git push origin main` that grep finds is inside a
   Write-Host string literal - printed advice for the operator, never run.
   There is no push path to guard because there is no push path at all.

==============================================================================
PACKAGING: marketplace committed, manifest 1.0.0, both validated, guard live,
nothing pushed.
==============================================================================
