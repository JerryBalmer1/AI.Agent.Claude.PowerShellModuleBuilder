==============================================================================
FALSIFYING Compare-TfGraph.ps1 STAGE 0 - THE DUPLICATE-ID REPAIR
Generated 2026-09-02T17:26:03Z
Fixture: fixture2
Oracle:  C:/__Code/__AI.Agent.Claude.PowerShellModuleBuilder/AI.Agent.Claude.PowerShellModuleBuilder/evals/tf/fixture2/expected-graph.json
==============================================================================

LEDGER backlog 32: the comparator could not see a duplicate node id, and a
producer that emitted one scored clean. Both graphs were keyed into ordered
dictionaries by id, so a duplicate overwrote its own entry ON BOTH SIDES and
even the oracle-against-itself control stayed green over a document that had
grown by a node. A DICTIONARY IS A DEDUPLICATOR.

The repair is Stage 0: uniqueness is asserted on both graphs before the first
assignment into a hashtable. This report is that repair, falsified - in both
directions, because a duplicated id is the same defect whichever side has it.

------------------------------------------------------------------------------
STEP 1 - CONTROLS. Both CLEAN oracles against themselves.

A repair that added detection by making every document differ from itself
would pass every step below and be worthless. These two are what say the
detections further down are detections and not noise.

  fixture1   IsMatch=True  differences=0  duplicated ids=0  over 78 nodes / 59 edges
  CONTROL GREEN: fixture1
  fixture2   IsMatch=True  differences=0  duplicated ids=0  over 99 nodes / 88 edges
  CONTROL GREEN: fixture2

------------------------------------------------------------------------------
THE MUTATION - mutation 8, duplicate-id.

CHANGED:  duplicate one node under its own id, byte-identical. Identical on
          purpose: a copy with a changed field would ALSO be detectable as a
          WrongAttribute on whichever copy the dictionary kept, and a
          mutation detected by two mechanisms cannot tell you which one you
          have.
LANDED:   document changed, 26384 -> 26517 chars
ID:       TfSiteOps:modules/common#output.tag

------------------------------------------------------------------------------
STEP 2 - PRODUCER SIDE. The clean oracle as expected; the duplicate-id graph
as the graph under test. This is the direction that matters for scoring: a
producer emitting an ambiguous id used to score clean.

IsMatch:          False
DifferenceCount:  1
duplicated ids:   expected side 0, actual side 1
  DuplicateId     TfSiteOps:modules/common#output.tag
                  on the actual side: the id occurs 2 times; every comparison keyed on it is ambiguous
DUPLICATE-ID: detected on producer side

------------------------------------------------------------------------------
STEP 3 - ORACLE SIDE. The MUTATED graph written to a scratch file and passed
as the oracle; the clean oracle on disk as the graph under test. This is the
direction pass 0034 discovered by accident: a broken oracle used to agree
with everything, including itself.

The fixture on disk is NOT written to. The scratch file is created in the OS
temp directory and removed in a finally.

SCRATCH ORACLE: C:/Users/jlbal/AppData/Local/Temp/tf-duplicate-id-0bd5ec7d/mutated-oracle.json
LANDED:   the scratch oracle reads back with 1 duplicated id(s)

IsMatch:          False
DifferenceCount:  1
duplicated ids:   expected side 1, actual side 0
  DuplicateId     TfSiteOps:modules/common#output.tag
                  on the expected side: the id occurs 2 times; every comparison keyed on it is ambiguous
DUPLICATE-ID: detected on oracle side

The scratch oracle has been removed. Both fixtures on disk are unchanged.

------------------------------------------------------------------------------
STEP 4 - THE CONTROLS AGAIN, after everything above.

Cheap, and it closes the one way this report could lie: a probe that left a
fixture edited behind it would have produced its detections honestly and
broken the instrument for everyone after.

  fixture1   IsMatch=True  differences=0  78 nodes / 59 edges
  fixture2   IsMatch=True  differences=0  99 nodes / 88 edges

==============================================================================
MUTATION 8: detected in BOTH directions, controls green before and after.
==============================================================================
